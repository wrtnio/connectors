/**
 * @packageDocumentation
 * @module api.functional.workflow.runs
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Resolved, Primitive } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type {
  IGetWorkflowRunsInput,
  IGetWorkflowRunsOutput,
} from "../../../structures/workflow/Workflow";

/**
 * Return all workflow run information
 *
 * @tag Workflow
 * @internal
 *
 * @controller WorkflowController.getWorkflowRuns
 * @path GET /workflow/runs
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getWorkflowRuns(
  connection: IConnection,
  input: getWorkflowRuns.Query,
): Promise<getWorkflowRuns.Output> {
  return !!connection.simulate
    ? getWorkflowRuns.simulate(connection, input)
    : PlainFetcher.fetch(connection, {
        ...getWorkflowRuns.METADATA,
        path: getWorkflowRuns.path(input),
      });
}
export namespace getWorkflowRuns {
  export type Query = Resolved<IGetWorkflowRunsInput>;
  export type Output = Primitive<IGetWorkflowRunsOutput>;

  export const METADATA = {
    method: "GET",
    path: "/workflow/runs",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = (input: getWorkflowRuns.Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(input as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/workflow/runs";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<IGetWorkflowRunsOutput>> =>
    typia.random<Primitive<IGetWorkflowRunsOutput>>(g);
  export const simulate = (
    connection: IConnection,
    input: getWorkflowRuns.Query,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(input),
      contentType: "application/json",
    });
    assert.query(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
