/**
 * @packageDocumentation
 * @module api.functional.connector.google_search.wanted
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { IGoogleSearch } from "../../../../structures/connector/google_search/IGoogleSearch";

/**
 * 원티드에서 채용 공고를 검색합니다.
 *
 * @summary 원티드 채용 공고 검색
 * @param input 검색 조건
 * @returns 원티드 채용 공고 검색 결과
 *
 * @controller GoogleSearchController.searchForWanted
 * @path POST /connector/google-search/wanted
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function searchForWanted(
  connection: IConnection,
  input: searchForWanted.Input,
): Promise<searchForWanted.Output> {
  return !!connection.simulate
    ? searchForWanted.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...searchForWanted.METADATA,
          template: searchForWanted.METADATA.path,
          path: searchForWanted.path(),
        },
        input,
      );
}
export namespace searchForWanted {
  export type Input = Primitive<IGoogleSearch.IRequest>;
  export type Output = Primitive<Array<IGoogleSearch.IResponse>>;

  export const METADATA = {
    method: "POST",
    path: "/connector/google-search/wanted",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/google-search/wanted";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<Array<IGoogleSearch.IResponse>>> =>
    typia.random<Primitive<Array<IGoogleSearch.IResponse>>>(g);
  export const simulate = (
    connection: IConnection,
    input: searchForWanted.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
