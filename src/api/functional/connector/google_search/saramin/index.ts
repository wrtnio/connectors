/**
 * @packageDocumentation
 * @module api.functional.connector.google_search.saramin
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { IGoogleSearch } from "../../../../structures/connector/google_search/IGoogleSearch";

/**
 * 사람인에서 채용 공고를 검색합니다.
 *
 * @summary 사람인 채용 공고 검색
 * @param input 검색 조건
 * @returns 사람인 채용 공고 검색 결과
 *
 * @controller GoogleSearchController.searchForSaramin
 * @path POST /connector/google-search/saramin
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function searchForSaramin(
  connection: IConnection,
  input: searchForSaramin.Input,
): Promise<searchForSaramin.Output> {
  return !!connection.simulate
    ? searchForSaramin.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...searchForSaramin.METADATA,
          template: searchForSaramin.METADATA.path,
          path: searchForSaramin.path(),
        },
        input,
      );
}
export namespace searchForSaramin {
  export type Input = Primitive<IGoogleSearch.IRequest>;
  export type Output = Primitive<Array<IGoogleSearch.IResponse>>;

  export const METADATA = {
    method: "POST",
    path: "/connector/google-search/saramin",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/google-search/saramin";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<Array<IGoogleSearch.IResponse>>> =>
    typia.random<Primitive<Array<IGoogleSearch.IResponse>>>(g);
  export const simulate = (
    connection: IConnection,
    input: searchForSaramin.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
