/**
 * @packageDocumentation
 * @module api.functional.connector.figma.get_comments
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { GetCommentsResponse } from "@figma/rest-api-spec/dist/api_types";
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { IFigma } from "../../../../structures/connector/figma/IFigma";

/**
 * 피그마 댓글들을 가져옵니다.
 *
 * @param input 댓글을 가져오기 위한 조건 값.
 * @summary 피그마 댓글 가져오기.
 * @returns 피그마 댓글 목록.
 * @tag figma
 *
 * @controller FigmaController.readComments
 * @path POST /connector/figma/get-comments
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function readComments(
  connection: IConnection,
  input: readComments.Input,
): Promise<readComments.Output> {
  return !!connection.simulate
    ? readComments.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...readComments.METADATA,
          template: readComments.METADATA.path,
          path: readComments.path(),
        },
        input,
      );
}
export namespace readComments {
  export type Input = Primitive<IFigma.IReadCommentInput>;
  export type Output = Primitive<GetCommentsResponse>;

  export const METADATA = {
    method: "POST",
    path: "/connector/figma/get-comments",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/figma/get-comments";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<GetCommentsResponse>> =>
    typia.random<Primitive<GetCommentsResponse>>(g);
  export const simulate = (
    connection: IConnection,
    input: readComments.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
