/**
 * @packageDocumentation
 * @module api.functional.connector.rag.generate
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { Try } from "../../../../../utils/createResponseForm";
import type { IRag } from "../../../../structures/connector/rag/IRag";

export * as sse from "./sse";

/**
 * RAG 분석을 기반으로 채팅을 합니다.
 *
 * @summary RAG 기반 채팅.
 * @param input
 * @tag RAG
 *
 * @controller RagController.generateChat
 * @path POST /connector/rag/generate
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function generateChat(
  connection: IConnection,
  input: generateChat.Input,
): Promise<generateChat.Output> {
  return !!connection.simulate
    ? generateChat.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...generateChat.METADATA,
          template: generateChat.METADATA.path,
          path: generateChat.path(),
        },
        input,
      );
}
export namespace generateChat {
  export type Input = Primitive<IRag.IGenerateInput>;
  export type Output = Primitive<Try<IRag.IGenerateOutput>>;

  export const METADATA = {
    method: "POST",
    path: "/connector/rag/generate",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/rag/generate";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<Try<IRag.IGenerateOutput>>> =>
    typia.random<Primitive<Try<IRag.IGenerateOutput>>>(g);
  export const simulate = (
    connection: IConnection,
    input: generateChat.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
