/**
 * @packageDocumentation
 * @module api.functional.connector.hwp.parse
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { IHwp } from "../../../../structures/connector/hwp/IHwp";

/**
 * hwp 파일을 파싱합니다.
 *
 * @summary Hwp 파일 파싱
 * @param input 파싱할 hwp 파일
 * @returns 파싱된 hwp 파일 텍스트 데이터.
 * @tag Hwp 한글 워드 프로세서
 *
 * @controller HwpController.parseHwp
 * @path POST /connector/hwp/parse
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function parseHwp(
  connection: IConnection,
  input: parseHwp.Input,
): Promise<parseHwp.Output> {
  return !!connection.simulate
    ? parseHwp.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...parseHwp.METADATA,
          path: parseHwp.path(),
        },
        input,
      );
}
export namespace parseHwp {
  export type Input = Primitive<IHwp.IParseInput>;
  export type Output = Primitive<IHwp.IParseOutput>;

  export const METADATA = {
    method: "POST",
    path: "/connector/hwp/parse",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/hwp/parse";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<IHwp.IParseOutput>> =>
    typia.random<Primitive<IHwp.IParseOutput>>(g);
  export const simulate = (
    connection: IConnection,
    input: parseHwp.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
