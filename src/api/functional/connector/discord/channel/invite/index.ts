/**
 * @packageDocumentation
 * @module api.functional.connector.discord.channel.invite
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { IDiscord } from "../../../../../structures/connector/discord/IDiscord";

/**
 * @controller DiscordController.inviteChnnel
 * @path POST /connector/discord/channel/invite
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function inviteChnnel(
  connection: IConnection,
  input: inviteChnnel.Input,
): Promise<inviteChnnel.Output> {
  return !!connection.simulate
    ? inviteChnnel.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...inviteChnnel.METADATA,
          path: inviteChnnel.path(),
        },
        input,
      );
}
export namespace inviteChnnel {
  export type Input = Primitive<IDiscord.IDiscordInviteChannelInput>;
  export type Output = Primitive<IDiscord.IDiscordInviteChannelOutput>;

  export const METADATA = {
    method: "POST",
    path: "/connector/discord/channel/invite",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/discord/channel/invite";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<IDiscord.IDiscordInviteChannelOutput>> =>
    typia.random<Primitive<IDiscord.IDiscordInviteChannelOutput>>(g);
  export const simulate = (
    connection: IConnection,
    input: inviteChnnel.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
