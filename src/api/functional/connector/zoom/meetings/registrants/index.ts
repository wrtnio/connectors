/**
 * @packageDocumentation
 * @module api.functional.connector.zoom.meetings.registrants
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { IZoom } from "../../../../../structures/zoom/IZoom";

/**
 * @controller ZoomController.addMeetingRegistrant
 * @path POST /connector/zoom/meetings/:meetingId/registrants
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function addMeetingRegistrant(
  connection: IConnection,
  meetingId: number,
  input: addMeetingRegistrant.Input,
): Promise<addMeetingRegistrant.Output> {
  return !!connection.simulate
    ? addMeetingRegistrant.simulate(connection, meetingId, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...addMeetingRegistrant.METADATA,
          path: addMeetingRegistrant.path(meetingId),
        },
        input,
      );
}
export namespace addMeetingRegistrant {
  export type Input = Primitive<IZoom.IAddMeetingRegistrantInput>;
  export type Output = Primitive<never>;

  export const METADATA = {
    method: "POST",
    path: "/connector/zoom/meetings/:meetingId/registrants",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = (meetingId: number) =>
    `/connector/zoom/meetings/${encodeURIComponent(meetingId ?? "null")}/registrants`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<never>> => typia.random<Primitive<never>>(g);
  export const simulate = (
    connection: IConnection,
    meetingId: number,
    input: addMeetingRegistrant.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(meetingId),
      contentType: "application/json",
    });
    assert.param("meetingId")(() => typia.assert(meetingId));
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
