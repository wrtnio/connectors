/**
 * @packageDocumentation
 * @module api.functional.connector.kakao_talk.get_calendars
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { ICommon } from "../../../../structures/connector/common/ISecretValue";
import type { IKakaoTalk } from "../../../../structures/connector/kakao_talk/IKakaoTalk";

/**
 * 카카오톡 캘린더 목록들을 모두 조회합니다.
 *
 * @summary 카카오톡 캘린더 목록 조회.
 * @param input 캘린더를 조회하기 위한 요청 DTO.
 * @returns 캘린더 목록 객체.
 * @tag 카카오톡
 *
 * @controller KakaoTalkController.getCalendars
 * @path POST /connector/kakao-talk/get-calendars
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getCalendars(
  connection: IConnection,
  input: getCalendars.Input,
): Promise<getCalendars.Output> {
  return !!connection.simulate
    ? getCalendars.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getCalendars.METADATA,
          template: getCalendars.METADATA.path,
          path: getCalendars.path(),
        },
        input,
      );
}
export namespace getCalendars {
  export type Input = Primitive<ICommon.ISecret<"kakao", ["talk_calendar"]>>;
  export type Output = Primitive<IKakaoTalk.IGetCalendarOutput>;

  export const METADATA = {
    method: "POST",
    path: "/connector/kakao-talk/get-calendars",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/kakao-talk/get-calendars";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<IKakaoTalk.IGetCalendarOutput>> =>
    typia.random<Primitive<IKakaoTalk.IGetCalendarOutput>>(g);
  export const simulate = (
    connection: IConnection,
    input: getCalendars.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
