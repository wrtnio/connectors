/**
 * @packageDocumentation
 * @module api.functional.connector.kakao_talk.memo.feed
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { Try } from "../../../../../../utils/createResponseForm";
import type { IKakaoTalk } from "../../../../../structures/connector/kakao_talk/IKakaoTalk";

/**
 * 카카오톡 내게 쓰기로 메시지를 보냅니다.
 *
 * @summary 카카오톡 내게 쓰기
 * @param input 메시지를 보내기 위한 조건
 * @returns 응답 코드
 * @tag 카카오톡
 *
 * @controller KakaoTalkController.feedMemo
 * @path POST /connector/kakao-talk/memo/feed
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function feedMemo(
  connection: IConnection,
  input: feedMemo.Input,
): Promise<feedMemo.Output> {
  return !!connection.simulate
    ? feedMemo.simulate(connection, input)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...feedMemo.METADATA,
          template: feedMemo.METADATA.path,
          path: feedMemo.path(),
        },
        input,
      );
}
export namespace feedMemo {
  export type Input = Primitive<IKakaoTalk.ISendKakaoTalkFeedInput>;
  export type Output = Primitive<Try<IKakaoTalk.IMemoOutput>>;

  export const METADATA = {
    method: "POST",
    path: "/connector/kakao-talk/memo/feed",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = () => "/connector/kakao-talk/memo/feed";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): Resolved<Primitive<Try<IKakaoTalk.IMemoOutput>>> =>
    typia.random<Primitive<Try<IKakaoTalk.IMemoOutput>>>(g);
  export const simulate = (
    connection: IConnection,
    input: feedMemo.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(),
      contentType: "application/json",
    });
    assert.body(() => typia.assert(input));
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
