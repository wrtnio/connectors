import { ITikTok } from "./ITikTok";

export namespace TikTok {
    export interface IVideoUploadInput {
        // Define the structure for video upload input
    }

    export interface IVideoDetailOutput {
        // Define the structure for video detail output
    }
}
